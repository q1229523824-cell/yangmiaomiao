# 开发进度

更新时间：2026-09-12

## 2026-09-12 三餐内提前安排 0.3.0（代码、模拟器与开发版上传完成）

### 阶段 1：统一计划与原生页面（完成）

- 首页依次选择日期、成员，在早餐/午餐/晚餐卡片内选“自己做 / 外卖”，不再跳到独立页；允许两人不同吃法。
- 支持今天至未来第 14 天，快捷条展示一周；选好即保存。按日期恢复已有计划，重开、跨天和换自炊菜谱不取消外卖安排。
- 增加 3 种早餐搭配，总目录 12 种。外卖按餐保存精简快照，保留原自炊食材；返回自炊恢复原分量。购物只算自炊，营养按来源合计估算区间，未选餐明确提示部分待选。
- schema 4 兼容 v1/v2/v3 存储和备份；旧参考不自动转成计划。历史优先保留今天/未来各日期最新选用版本；恢复旧版本后切日期回来仍保留该选择。

### 阶段 2：测试、错误修复与截图（完成）

- 最终源码 `npm run verify:all` 通过：26 个文件、357 项测试，5 WXML / 6 WXSS 官方编译通过。覆盖跨日期、三餐来源、双人混合营养/购物、禁忌重检、失效点击、保存失败、旧备份迁移及 60 份混合计划容量。
- 发现“恢复旧安排后切日期回去选中较新版本”的边界，先补测试复现，再在历史恢复时提升该版本为当日选用版本，不丢弃其他历史；新增测试已通过。
- 初轮模拟器报告 `artifacts/simulator/2026-09-12T06-06-35-427Z/report.json` 因原生节点未就绪退出，原存储已恢复。自动化修正同路由重启时等待新页面 ID，并只重试元素读取，不重复执行点击。
- 第二轮报告 `artifacts/simulator/2026-09-12T06-10-09-069Z/report.json` 为 22/22 通过。历史恢复边界修正后的第三轮发现取消确认框测试存在竞态（回执在点击实际处理前返回）；报告 `2026-09-12T06-15-41-353Z` 确认原数据恢复。自动化改为先观察确认框实际调用，再断言取消和切换下一次确认结果，不靠短暂未写入推断取消完成。
- 最终报告 `artifacts/simulator/2026-09-12T06-18-08-277Z/report.json` 为 22/22 通过，`success: true`、`restored: true`、`exceptions: []`。提前日期、早餐外卖、午餐折叠卡片和新禁忌待选截图已目视检查；未捕获应用异常，原模拟器存储已恢复。
- `npm run preview:takeout` 已验证 12 种组合和默认两位成员三餐匹配。模拟器网络为 API 故障注入，剪贴板为隔离模拟，真实手机仍需回归。

### 阶段 3：文档与交付

- README、架构、隐私和外卖说明已更新到新主流程；未新增云服务、网络或设备权限。
- CLI 已使用 AppID `wxc4ab98745c2f2533` 上传开发版本 `0.3.0`，包大小 231,006 字节，备注“三餐内选择自炊或外卖，支持提前两周排餐和购物清单联动”；回执 `artifacts/upload-0.3.0.json`。
- 本轮源码与文档随提交交付到 `q1229523824-cell/yangmiaomiao`；提交及 GitHub CI 结果以对应提交的记录为准，不复用 0.2.0 的成功结论。
- 本轮没有选择公众平台的 0.3.0 体验版、提交审核或正式发布。手机试用需先在版本管理将 0.3.0 选为体验版，再使用具备体验权限的微信扫码；真实剪贴板、触控与飞行模式待手机验证。

## 2026-09-12 外卖推荐 0.2.0（代码、模拟器与开发版上传完成）

- “今日”页新增“外卖怎么点”入口，原四个 Tab 保留；内置9种明确份量的点单组合，可分别查看两位成员的午/晚餐。
- 支持分类、可编辑热量/蛋白筛选、收藏、点单建议/搜索词复制，以及按日期/成员/餐次保存参考。记录不替换自炊菜单、不混入其营养合计或购物清单。
- 热量上界与蛋白下界同时满足才匹配；主食材、潜在汤底/调料共同执行禁忌。有过敏记录时暂停个性化外卖推荐，避免将未知店铺当成已核验。
- schema 3 对旧 v1/v2 存储和备份迁移；外卖字段校验、60条记录上限、50项收藏上限、去重保存和事务串行化已覆盖。
- `npm run verify:all` 通过：24个测试文件、333项测试，官方编译器5个 WXML、6个 WXSS 通过；`npm run preview:takeout` 可重现实际估算和默认档案匹配结果。
- 第一轮模拟器在旧剪贴板回归收到空内容后退出，报告 `artifacts/simulator/2026-09-12T04-24-41-230Z/report.json` 记录 `restored: true`。剪贴板测试改用隔离模拟数据，避免系统剪贴板干扰；随后修正自动化等待事件/页面过渡的时机。最终报告 `artifacts/simulator/2026-09-12T04-28-26-708Z/report.json` 为19/19通过、`success: true`、`restored: true`、`exceptions: []`；外卖目标、点单详情和过敏阻断截图已目视检查。真实手机剪贴板及飞行模式仍待验收。
- CLI 已使用 AppID `wxc4ab98745c2f2533` 上传开发版本 `0.2.0`，包大小204,043字节，备注“新增离线外卖点单推荐、营养区间、收藏与本餐参考”；本地回执 `artifacts/upload-0.2.0.json`。本次没有执行公众平台的体验版选择、版本审核或正式发布，不能把旧体验二维码当作新版证据。
- README、PRIVACY、架构和 `docs/TAKEOUT.md` 已说明来源、估算假设与实际营养/商家能力边界；平台版本上传与正式发布分别记录。

## 2026-09-08 收尾进度（代码与自动化验收已完成）

- 微信开发者工具自动化已在真实 AppID 下打开四个原生页面并完成 16 项核心交互检查；环境为开发者工具 2.02.2608060、基础库 3.16.2、390px 模拟器。
- 模拟器回归已覆盖首次生成、重启/切 Tab 不刷新、跨日期提示、番茄硬排除、鱼类排除与猪里脊偏好、档案校验与主动重算、购物勾选、历史取消/恢复、过敏原安全恢复、无可行菜单、损坏缓存和网络 API 故障注入。
- 最新报告 `artifacts/simulator/2026-09-08T08-23-20-588Z/report.json` 显示 16/16 通过、`restored: true`、`exceptions: []`；原模拟器存储已恢复。
- 已修复模拟器页面栈切换、重复偏好异步落盘和 checkbox-group 事件的兼容性问题；本轮回归没有再出现“不要番茄”写入丢失。
- 代码、类型检查、本地测试和开发者工具模板编译现已收口；剩余工作仅是实际手机断网/触控验收以及公众平台审核发布。

## 2026-09-08 离线迁移增强

- 档案页新增“复制本地备份”和“从剪贴板恢复”，不引入云服务或网络 API；备份只包含最近一次已保存的完整 `AppState`。
- 备份使用显式格式标记、schema 版本和导出时间；导入先限制大小、解析 JSON、拒绝非本项目格式/未来版本，并复用当前状态校验及 v1 迁移逻辑。
- 导入需要用户确认后才通过共享存储队列显式替换本机数据，即使旧缓存损坏也能恢复；空文本、损坏文本、无效状态和取消确认都不会写入。
- 新增本地备份领域测试与档案页控制器测试；`npm run verify:all` 当前通过 21 个测试文件、262 项测试，微信官方检查仍为 4 个 WXML、5 个 WXSS。
- 隐私说明、README、架构和发布清单已明确剪贴板备份包含身体数据、仅用户主动触发、不是云备份。

## 2026-09-08 新账号迁移与体验版交付

- 项目配置已切换到新 AppID `wxc4ab98745c2f2533`；CLI 登录态为 `login: true`，并成功打开项目配置。
- `npm run verify:all` 在切换配置后再次通过：TypeScript、21 个测试文件/262 项测试，以及微信 `wcc/wcsc` 的 4 个 WXML、5 个 WXSS 检查均通过。
- 已使用新 AppID 上传当前代码 `0.1.2`，上传包大小 152,925 字节，备注为“离线双人健身饮食体验版（本地备份恢复）”；上传工具输出确认使用 `wxc4ab98745c2f2533`。
- 新公众平台版本管理已将 `0.1.2` 设置为体验版，入口 `pages/index/index`；体验二维码已展示，页面提示有效至 2026-09-15 前。
- 新账号当前体验成员数为 0；需要管理员在“成员管理 → 体验成员”中重新添加 `yxr_6211` 后，才能用该微信号扫码体验。
- 用户已确认新账号后台的小程序服务类目审核通过，类目门槛已解除。

## 已完成：离线产品核心

- 建立原生微信小程序 + TypeScript 工程和四个 Tab 页面。
- 将食材、菜谱、营养、偏好、排餐、购物和快照逻辑拆为可测试模块。
- 一份共同菜单只在每位成员的克数上产生差异；页面、营养和购物清单读取同一 `DailyPlan` 快照。
- 身高、体重、年龄、活动量和目标强度均可编辑、逐字段校验并持久化；重新计算后真实影响目标与份量。
- 打开、切换页面及跨日期不会自动刷新已保存菜单；档案修改后保留旧快照并提示主动重算。
- 扩充常见食材和同义词解析；“不要番茄/西红柿”、鱼类、牛肉等明确排除进入硬约束。
- 多意图和否定范围回归测试覆盖“不要番茄，想吃猪里脊”；重复偏好、无标点混合过敏声明和不支持复合词都按保守规则处理。
- “少放番茄”会明确按“不放番茄”处理，“少放辣/不要辣”不会新增辣味偏好并会取消已有辣味优先；“空气炸锅坏了”不会被当成做法偏好。
- 偏好标签支持逐项移除和普通偏好重置；明确过敏/不耐受形成独立过滤限制，普通重置和历史恢复不会静默解除。
- 六种已建模过敏原的 64 种组合跨 32 个种子验证；新增无蛋早餐，普通燕麦和荞麦面保守标记为麸质风险，有过敏记录时隐藏调味建议。
- 本地存储使用命名空间和 schemaVersion；缺失键才使用默认值，损坏/未来版本/迁移失败会保留原缓存并停止写入，用户确认后才可清除。
- 历史页展示最多 60 份快照，并在用户确认后恢复所选菜单及当时普通偏好，同时保留当前过敏原过滤记录。
- 热量差额和蛋白缺口分散到午晚餐；超出本地模板的目标或单份上限时明确拒绝生成，不展示极端份量。
- 首页、档案、购物和历史页均提供对应的加载、空状态、忙碌或错误反馈。

## 自动化验证

2026-09-08 最近一次执行 `npm run verify`：

- TypeScript `tsc --noEmit`：通过；
- Vitest：21 个测试文件、262 项测试全部通过；
- 覆盖领域计算、随机种子硬禁忌、64 种过敏原组合、页面控制器冷启动、本地存储保全、状态生命周期、历史恢复和结构/离线 API 静态扫描。
- 代表性最长称呼、完整购物勾选和 60 份历史快照的 JSON 信封为 275,920 字节，存储预算回归线为 512 KiB。

微信平台验证目前分为三层：

1. `npm run verify`：通过，证明 TypeScript 和本地自动化测试通过。
2. `npm run verify:wechat`：已在微信开发者工具 `2.02.2608060` 上通过，官方 `wcc/wcsc` 共验证 4 个 WXML 和 5 个 WXSS 文件。
3. 完整项目/模拟器：此前账号已完成。`npm run verify:simulator` 在已登录开发者工具中完成 16 项交互检查，最新报告显示无异常且原存储已恢复。切换到新 AppID `wxc4ab98745c2f2533` 后需重新确认开发者工具绑定和上传；模拟器的网络故障注入通过，但这不能替代 iPhone/Android 飞行模式实测。

第二层只验证原生模板和样式语法，不能证明真实 AppID 下的完整项目可启动，也不能替代交互和真机验收。

## 已完成：发布材料的本地版本

- README 已更新到当前功能和真实离线边界，包含本地剪贴板备份说明。
- 增加架构、隐私和发布检查清单。
- 增加 Node.js 22 的 GitHub Actions CI 配置。
- `.gitignore` 已排除本地私有配置、依赖、覆盖率、编译产物、环境文件和密钥文件。
- 文档不包含原始 HTML 的个人临时绝对路径。
- 默认身高体重已替换为明确虚构的演示值，真实档案只会在使用时写入设备本地存储。
- 源码已提交到 `main` 并普通推送至 `q1229523824-cell/yangmiaomiao`，没有覆盖远端历史；本轮新账号迁移记录已随提交 `63b01da` 同步。
- 上一版核心代码提交 `fb12d39` 对应的远端 GitHub Actions CI（run `34185028324`）已完成并通过；本轮增强提交 `24a5cae` 与文档提交 `5976a6f` 已推送到 `main`，对应 CI（run `34204480796`、`34204535800`）均已成功。
- 2026-09-08 新账号 `wxc4ab98745c2f2533` 已完成 `0.1.2` 上传和体验版设置；上传包大小 152,925 字节，备注“离线双人健身饮食体验版（本地备份恢复）”，入口 `pages/index/index`，二维码页面提示有效至 2026-09-15 前。

## 尚未完成

- iPhone 与 Android 的断网真机验证。
- 在新账号添加体验成员 `yxr_6211`，再让该成员使用微信扫一扫体验版二维码，并记录扫码、断网和回归结果。
- 后续平台审核、审核通过后的正式发布。

这些项目需要开发者工具中的人工操作或设备参与，不能以 Node.js 自动化测试结果代替。

## 2026-09-08 发布准备记录

2026-09-08 已核对正式发布门槛并写入 `RELEASE_CHECKLIST.md`；用户已决定按个人主体推进。代码、此前账号模拟器验收、新账号上传、体验版二维码和服务类目审核均已完成；下一步由管理员确认名称/图标、隐私指引和备案提示，添加新账号体验成员并完成真机回归，再提交审核和审核后的发布。正式发布按钮尚未点击。

2026-09-07 此前账号曾添加 1 名体验成员并上传 `0.1.0`；该记录仅作历史追踪，新账号当前体验成员数为 0，需要重新添加 `yxr_6211`。

2026-09-08 曾在此前账号上传 `0.1.1` 版本，上传包大小 145,776 字节；该版本记录保留作历史追踪，新账号将重新上传当前代码的 `0.1.2`。

2026-09-07 23:04 旧手机预览二维码扫码提示失效；原因是开发版预览码不能替代体验版二维码。随后已通过开发者工具 CLI 上传 `0.1.0`（包大小 141,067 字节）并配置为体验版，已生成新的体验版二维码。体验成员应在微信内扫码，不要使用其他应用的扫码器；若仍提示无权限，先核对扫码微信号是否为已加入的体验成员。
