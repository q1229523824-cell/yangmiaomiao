# 发布检查清单

本清单同时记录“已由自动化证明”和“仍需人工/账号证明”的项目。只有实际执行并保存证据后才能勾选，不得用本地单元测试替代模拟器或真机结论。

## 1. 本地代码门禁

- [x] `npm run verify` 通过（2026-09-07：12 个测试文件、96 项测试）。
- [x] 页面结构测试确认 `app.json` 声明的四个页面文件齐全。
- [x] 静态检查未发现小程序运行时网络 API 或常见秘密字面量。
- [x] README、架构、隐私和本发布清单已建立。
- [x] 在最终提交内容上重新执行 `npm ci` 和 `npm run verify:all`。
- [x] `npm run verify:wechat` 已在开发者工具 `2.02.2608060` 通过：4 个 WXML、5 个 WXSS。

## 2. 账号与项目配置

- [ ] 准备属于该产品的真实小程序 AppID。
- [ ] 使用有权限的微信账号登录微信开发者工具。
- [ ] 导入仓库根目录，并确认项目根为 `miniprogram/`。
- [ ] 将占位 `touristappid` 替换为真实 AppID 后编译。
- [ ] 确认 AppSecret、访问令牌和私钥没有进入前端或 Git。

`touristappid` 只是仓库占位，不能作为真实身份完成登录态模拟器、真机预览、上传和发布。

当前明确证据：CLI `islogin` 返回未登录；占位配置的 `cli open` 返回 code 10：“不存在此 AppID”。取得真实 AppID 并登录后，必须重新执行完整项目打开和模拟器检查。

## 3. 模拟器核心流程

- [ ] 清空本项目本地数据后，首次进入只生成一份菜单。
- [ ] 返回页面、切换四个 Tab 和再次进入都不会自动换菜。
- [ ] 跨日期打开时保留旧菜单，并出现日期提示。
- [ ] 主动换菜后生成新快照，午晚餐两人菜品相同、克数分别显示。
- [ ] 输入“不要番茄”，新菜单、餐项和购物清单都没有番茄。
- [ ] 输入“不要鱼，想吃猪里脊”，鱼类被排除且猪里脊不被误伤。
- [ ] 重复相同偏好不重生成菜单；单项删除和普通重置可用。
- [ ] 明确牛奶/海鲜过敏会形成硬约束；普通重置保留，只有明确取消表述才解除。
- [ ] 当前过敏原不会被历史恢复静默删除，含当前过敏原的旧菜单不能被误用。
- [ ] 无可用菜谱时明确提示，旧的不安全菜单被隐藏。
- [ ] 身高、体重、年龄的空值、非数字、整数要求和上下界提示正确。
- [ ] 修改活动量或目标强度并保存后，旧菜单保留；主动重算后目标与份量改变。
- [ ] 购物清单勾选在切换页面后仍保留。
- [ ] 历史最多展示 60 份；确认恢复后菜单和当时偏好同时恢复。
- [ ] 取消历史恢复不会改变当前状态。
- [ ] 清除本地数据需要确认，且不会删除其他应用数据。
- [ ] 加载、空状态、忙碌、存储失败提示和重试入口可见。

## 4. 离线与真机

- [ ] 模拟器先成功加载代码包，再断网完成档案、排餐、购物和历史流程。
- [ ] iPhone 真机先成功加载体验版，再断网重复核心流程。
- [ ] Android 真机先成功加载体验版，再断网重复核心流程。
- [ ] 记录机型、系统版本、微信版本、基础库版本、时间和结果。
- [ ] 检查小屏、长称呼、大字体、键盘遮挡、滚动和主要按钮触控区域。
- [ ] 确认系统清理微信存储后本地数据可能丢失，提示与实际一致。

## 5. 内容与合规

- [ ] 页面和发布说明不宣称医疗诊断、治疗或营养结果绝对准确。
- [ ] 页面和发布说明明确：调味品配料及交叉污染不在结构化过敏原过滤范围内。
- [ ] 隐私说明与实际数据字段、保存位置、删除方式一致。
- [ ] 若未来新增联网能力，先更新隐私说明和授权流程。
- [ ] 检查菜谱文本、图标、名称和原型素材具备发布权利。
- [ ] 选择正确服务类目，完成备案及微信要求的其他材料。

## 6. GitHub 与体验版交付

- [ ] `git status` 仅包含预期文件。
- [ ] 检查暂存 diff，不含个人临时绝对路径、未经同意的身体数据、私钥、令牌或 `project.private.config.json`。
- [x] 源码默认双人身高/体重已替换为明确虚构、可编辑的演示值。
- [ ] 由项目所有者决定许可证；未选择时不要擅自添加。
- [ ] 本地分支名对齐为 `main`，使用普通 push 推送目标空仓库。
- [ ] GitHub Actions 的 Node.js 22 `npm ci` 和 `npm run verify` 通过。
- [ ] 在真实 AppID 下上传体验版，并邀请指定体验成员扫码验证。
- [ ] 保存体验版版本号、上传时间及回归结果。
