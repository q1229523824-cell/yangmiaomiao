# 发布检查清单

本清单同时记录“已由自动化证明”和“仍需人工/账号证明”的项目。只有实际执行并保存证据后才能勾选，不得用本地单元测试替代模拟器或真机结论。

## 1. 本地代码门禁

- [x] `npm run verify` 通过（2026-09-08：21 个测试文件、262 项测试）。
- [x] 页面结构测试确认 `app.json` 声明的四个页面文件齐全。
- [x] 静态检查未发现浏览器 DOM、网络/云 API、远程运行时 URL 或常见秘密字面量。
- [x] README、架构、隐私和本发布清单已建立。
- [x] 在最终代码内容上重新执行 `npm ci` 和 `npm run verify:all`。
- [x] `npm run verify:wechat` 已在开发者工具 `2.02.2608060` 通过：4 个 WXML、5 个 WXSS。
- [x] 页面控制器冷启动/持久化、缓存损坏保全、六种过敏原组合和目标能力门禁均有自动化回归。
- [x] 本地备份文本的格式、版本、迁移、损坏输入和档案页复制/恢复流程有自动化回归；备份只走用户主动触发的剪贴板 API。

## 2. 账号与项目配置

- [x] 已配置项目所有者提供的真实小程序 AppID `wxd900965119113522`。
- [x] 使用有权限的微信账号登录微信开发者工具（CLI `islogin` 返回 `login: true`）。
- [x] 导入仓库根目录，并用 `cli open --project` 成功打开项目；项目根为 `miniprogram/`。
- [x] `project.config.json` 已使用真实 AppID；完整编译和交互仍需人工确认。
- [ ] 确认 AppSecret、访问令牌和私钥没有进入前端或 Git。

AppID 只用于标识小程序项目；云服务不是本项目运行所必需的，当前选择“不使用云服务”。

当前明确证据：CLI `islogin` 返回 `login: true`，使用真实 AppID 的 `cli open --project` 返回成功；`npm run verify:simulator` 已在开发者工具自动化模式完成项目启动、核心交互、剪贴板备份恢复和网络故障注入，16 项检查全部通过。仍需手机真机断网、触控和体验版扫码回归。

已生成真实 AppID 的手机预览二维码（2026-09-07，预览包 141,067 字节）；旧码曾提示失效。预览码属于临时开发码，不能代替体验版上传；当前已上传并设置真实体验版，体验二维码的扫码结果仍需记录。

## 3. 模拟器核心流程（已通过）

- [x] 清空本项目本地数据后，首次进入只生成一份菜单。
- [x] 返回页面、切换四个 Tab 和再次进入都不会自动换菜。
- [x] 跨日期打开时保留旧菜单，并出现日期提示。
- [x] 主动换菜后生成新快照，午晚餐两人菜品相同、克数分别显示。
- [x] 输入“不要番茄”，新菜单、餐项和购物清单都没有番茄。
- [x] 输入“不要鱼，想吃猪里脊”，鱼类被排除且猪里脊不被误伤。
- [x] 重复相同偏好不重生成菜单；单项删除和普通重置可用。
- [x] 明确牛奶/海鲜过敏会形成硬约束；普通重置保留，只有明确取消表述才解除。
- [x] 当前过敏原不会被历史恢复静默删除，含当前过敏原的旧菜单不能被误用。
- [x] 无可用菜谱时明确提示，旧的不安全菜单被隐藏。
- [x] 身高、体重、年龄的空值、非数字、整数要求和上下界提示正确。
- [x] 修改活动量或目标强度并保存后，旧菜单保留；主动重算后目标与份量改变。
- [x] 购物清单勾选在切换页面后仍保留。
- [x] 历史最多展示 60 份；确认恢复后菜单和当时偏好同时恢复。
- [x] 取消历史恢复不会改变当前状态。
- [x] 清除本地数据需要确认，且不会删除其他应用数据。
- [x] 加载、空状态、忙碌、存储失败提示和重试入口可见。

证据：`artifacts/simulator/2026-09-08T08-23-20-588Z/report.json`，16/16 通过，`restored: true`，`exceptions: []`。其中网络项是 API 故障注入，不能替代手机飞行模式。

## 4. 离线与真机

- [ ] 模拟器先成功加载代码包，再断网完成档案、排餐、购物和历史流程。
- [ ] iPhone 真机先成功加载体验版，再断网重复核心流程。
- [ ] Android 真机先成功加载体验版，再断网重复核心流程。
- [ ] 记录机型、系统版本、微信版本、基础库版本、时间和结果。
- [ ] 检查小屏、长称呼、大字体、键盘遮挡、滚动和主要按钮触控区域。
- [ ] 确认系统清理微信存储后本地数据可能丢失，提示与实际一致。
- [ ] 在两台设备上手动验证“复制本地备份 → 微信内转移文本 → 从剪贴板恢复”，并确认备份文本未发送到公开位置。

## 5. 内容与合规

- [ ] 页面和发布说明不宣称医疗诊断、治疗或营养结果绝对准确。
- [ ] 页面和发布说明明确：调味品配料及交叉污染不在结构化过敏原过滤范围内。
- [ ] 隐私说明与实际数据字段、保存位置、删除方式一致。
- [ ] 若未来新增联网能力，先更新隐私说明和授权流程。
- [ ] 检查菜谱文本、图标、名称和原型素材具备发布权利。
- [ ] 选择正确服务类目，完成备案及微信要求的其他材料。

## 6. GitHub 与体验版交付

- [x] `git status` 和提交文件清单仅包含预期文件。
- [x] 暂存内容不含个人临时绝对路径、真人默认身体数据、私钥、令牌或 `project.private.config.json`。
- [x] 源码默认双人身高/体重已替换为明确虚构、可编辑的演示值。
- [ ] 由项目所有者决定许可证；未选择时不要擅自添加。
- [x] 本地分支名对齐为 `main`，使用普通 push 推送目标空仓库，未 force push。
- [x] GitHub Actions 的 Node.js 22 `npm ci` 和 `npm run verify` 已通过（提交 `fb12d39`，run `34185028324`）。
- [x] 本轮提交 `24a5cae` 与 `5976a6f` 的 GitHub Actions 已通过（run `34204480796`、`34204535800`）；本地 `npm run verify:all` 与真实 AppID 模拟器 16/16 也已通过。
- [x] 2026-09-07 在真实 AppID 下上传 `0.1.0` 并在公众平台设置为体验版；入口 `pages/index/index`，平台显示二维码有效至 2026-09-14。
- [x] 2026-09-08 使用真实 AppID 上传最新 `0.1.1`（145,776 字节，备注“离线双人健身饮食体验版（稳定性修复）”），并在公众平台选为体验版；入口 `pages/index/index`，体验二维码页面提示 2026-09-15 前有效。
- [ ] 让已加入的体验成员使用微信扫一扫体验版二维码，记录扫码成功、核心流程和断网结果。
- [x] 2026-09-07 已在公众平台添加 1 名体验成员，权限为“查看体验版”；账号标识不写入仓库。
- [x] 2026-09-07 已保存体验版版本号、上传时间和项目备注；真机回归结果仍待补充。

## 7. 正式发布前的外部材料与平台操作

以下事项不能由本地测试代替，需要由小程序管理员在微信公众平台确认或执行：

- [x] 决策：按个人主体推进（用户于 2026-09-07 确认）；仍需在公众平台核验账号的实际主体状态和可选类目。
- [ ] 确认管理员账号能登录公众平台；个人主体的可选服务类目和资质要求以后台当前列表为准。
- [ ] 确认小程序名称、头像/图标、简介、服务类目和客服联系方式；素材和菜谱内容具备使用或发布权利。
- [ ] 在后台类目列表中优先核对与本项目功能一致的“工具/健康管理”类目；仅描述身高体重记录、饮食计划、营养估算和购物清单，不选择医疗类目或写成诊断、治疗服务。可参考个人主体类目快照 <https://github.com/juanyun-ai/build-personal-wechat-mini-program/blob/main/references/subject-and-category-gate.md>，提交时以微信后台为准。
- [ ] 在后台如实填写用户隐私保护指引：本项目会在当前设备保存称呼、身高、体重、年龄、活动量、目标、饮食偏好、过敏原、菜单、购物勾选和历史快照；不调用手机号、头像、定位、相册、相机、麦克风或云开发能力。
- [ ] 按平台提示完成隐私指引同步/用户确认流程；若后台要求隐私授权弹窗，先在首次保存个人资料前完成官方流程。
- [ ] 在中国大陆提供互联网信息服务时，登录后台确认是否触发小程序备案/ICP备案要求，并在被要求时先完成备案；监管通知明确将小程序列入移动互联网应用程序备案范围，见 <https://www.gov.cn/zhengce/zhengceku/202308/content_6897341.htm?type=mobile-internet>。
- [ ] 体验版已上传；先让体验成员在 iPhone/Android 断网验证，再上传待审版本。
- [ ] 在微信公众平台“版本管理”中提交审核，填写功能说明、测试路径、测试账号（本项目无账号登录则注明“无需登录”）及补充材料；审核通过后由管理员点击“发布”。
- [ ] 保存正式版本号、审核结果、发布时间和线上二维码；发布后再次冷启动、断网和回归核心流程。

本项目当前不需要苹果 ID、AppSecret、服务器、域名、HTTPS 或云开发环境才能运行；正式发布仍需要有权限的微信小程序管理员完成平台操作。上传、审核和发布通常是“上传代码 → 提交审核 → 审核通过后发布”的连续流程，平台页面以当前公众平台显示为准，参考腾讯云对微信小程序发布流程的说明：<https://cloud.tencent.com/document/product/1301/55140>。
