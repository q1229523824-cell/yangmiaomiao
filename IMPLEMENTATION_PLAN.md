# 实施计划与验收标准

持续目标：把单文件 HTML 原型迁移为稳定、好用、核心功能离线可用的原生微信小程序，并将源码安全交付到目标 GitHub 仓库。

## 产品不变量

1. 自炊部分先确定共同菜谱和食材、按成员分配克数；各成员每餐可独立选择自炊或外卖，仍保存于同一日计划。
2. 页面、营养汇总和购物清单读取同一个不可变 `DailyPlan` 快照。
3. 过敏与明确排除是硬约束；无合法方案时显式失败，不能静默放回禁忌食材。
4. 打开、切换页面或自然跨天不能随机换菜；主动选计划日期时优先恢复已有安排，没有才生成。明确选吃法、换菜、重算或恢复历史才修改已保存计划。
5. 本地模式不调用网络，档案、偏好、购物状态和历史默认只存放在当前设备。
6. “离线饮食小助手”是规则解析器；营养数值必须由确定性程序计算。
7. 目标超出本地模板和单份称重上限时显式拒绝生成，不展示看似精确的极端份量。

## 2026-09-12：三餐内安排自炊 / 外卖（0.3.0，代码与开发版完成）

1. 首页每餐提供“自己做 / 外卖”，按成员独立选择；移除独立外卖主入口。
2. 日期选择支持提前两周规划，按日期恢复已有安排；反复打开、切日期、换自炊菜谱不取消已选外卖。
3. 外卖快照进入每日计划，营养按吃法汇总为估算区间；待选餐明确标记，购物仅统计自炊。
4. 增补早餐搭配；保留忌口硬过滤、收藏和点单复制，schema 4 兼容旧菜单与备份。
5. 回归跨日期、混合用餐、失败恢复、禁忌更新及模拟器原生操作；记录证据并更新 README。

验收：357 项测试、5 WXML / 6 WXSS 编译、22 项原生模拟器检查通过，原数据恢复且无应用异常；README 已更新，0.3.0 开发版上传成功（231,006 字节）。体验版选择与手机验证待完成，不代表正式发布。

## 2026-09-12 外卖推荐阶段（完成代码、模拟器与开发版上传）

1. 实现本地常见外卖点单组合和确定性筛选：按成员、午晚餐、热量上限与蛋白质下限推荐，显示估算区间和份量假设。
2. 继续执行食材/食物组硬排除；有过敏记录时暂停个性化外卖推荐，商家配料和交叉接触无法由离线目录核验。
3. 增加“外卖怎么点”页面、首页入口、分类、收藏、点单备注复制及本餐参考记录；外卖参考不替换自炊快照或计入其营养/购物合计。
4. 迁移旧本地存储及备份，覆盖禁忌、区间边界、无匹配、保存失败和重新打开的回归测试。
5. 运行全量检查和开发者工具模拟器，补齐 README、营养估算说明和进度；交付源码，平台体验包与正式发布分别记录。

验收：333项测试、5 WXML/6 WXSS 编译、19项模拟器回归通过；`0.2.0` 已上传新 AppID 开发版本（204,043字节）。手机真机、体验版选择和审核/发布状态待平台确认。

## 阶段 A：审计与验收基线（完成）

- [x] 检查源码、测试、依赖、Git 状态和目标仓库。
- [x] 确认目标 GitHub 仓库公开且为空，没有远端覆盖冲突。
- [x] 建立实施计划、进度和待办文档。
- [x] 建立页面完整性、禁用浏览器 DOM/网络 API 和常见凭据的静态检查。

证据：`npm run verify` 和项目结构测试。

## 阶段 B：三个核心修复（完成）

- [x] 身高、体重、年龄可编辑、逐字段校验并保存在本机。
- [x] 活动量和目标强度可编辑，并在明确重算后影响营养目标与份量。
- [x] 扩展常见主食、蔬菜、蛋奶和肉类的偏好解析。
- [x] “不要番茄/西红柿”形成 `tomato` 硬排除，跨随机种子验证不会进入菜谱或餐项。
- [x] 多意图否定范围不会误伤后续肯定偏好。
- [x] 重复偏好保持幂等，不触发无谓菜单重生成。
- [x] 偏好可逐项撤销，也可重置普通偏好并保留过敏原。
- [x] 已保存菜单跨页面、跨启动和跨日期保持不变。
- [x] 档案修改后保留旧菜单，并提示用户主动重算。

证据：偏好解析、随机种子、档案表单、排餐和生命周期测试。

## 阶段 C：可用产品体验（完成代码与本地测试）

- [x] 四个原生 Tab：今日、购物、档案、历史。
- [x] 历史页最多展示 60 份快照，可确认恢复菜单和当时偏好。
- [x] 当前菜单日期、旧菜单和待重算状态有明确反馈。
- [x] schemaVersion 迁移、损坏/未来缓存保全、历史上限和用户确认后的本地数据清除。
- [x] 页面覆盖加载、空状态、忙碌和错误反馈。

证据：应用层、展示模型、存储、页面结构测试。真实交互仍归阶段 D 验收。

## 阶段 D：微信运行与离线验收（模拟器完成，真机待验）

- [x] `npm run verify:wechat` 在开发者工具 `2.02.2608060` 上通过：4 个 WXML、5 个 WXSS。
- [x] 已配置新账号真实 AppID `wxc4ab98745c2f2533`；CLI `islogin` 返回 `login: true`，并已用新配置打开项目。
- [x] 此前账号曾生成手机预览二维码，预览包大小为 141,067 字节；新账号当前交付使用公众平台体验版二维码。
- [x] 使用此前真实 AppID 和登录态开发者工具完成模拟器核心流程；16 项检查全部通过，报告显示无异常且原存储已恢复。切换新账号后需再做一次 AppID 绑定检查。
- [x] 在模拟器内对 `request`、`downloadFile`、`uploadFile` 和 `connectSocket` 注入失败，核心流程仍全部通过，证明业务逻辑不依赖网络 API。
- [x] 新账号已上传当前 `0.1.2`，并在公众平台设置为体验版；入口为 `pages/index/index`，体验二维码页面提示有效至 2026-09-15 前。
- [ ] iPhone 与 Android 至少各完成一次断网检查。

当前剩余证据：iPhone/Android 真机断网、触控和体验版扫码记录，以及平台审核发布记录。模拟器脚本已覆盖开发者工具中的项目启动、页面交互和网络故障注入；Node.js 测试或底层 `wcc/wcsc` 仍不能替代真机证据。

## 阶段 E：仓库交付与发布准备（代码仓库交付完成）

- [x] README 写明定位、功能、结构、运行、测试、离线边界、隐私和非医疗声明。
- [x] 增加架构、隐私和发布检查文档。
- [x] 增加 Node.js 22 GitHub Actions，执行 `npm ci` 和 `npm run verify`。
- [x] 自动检查常见密钥字面量；`.gitignore` 排除本地私有配置和构建产物。
- [x] 完成最终暂存 diff 和个人路径人工复核。
- [x] 提交到 `main` 并普通推送目标 GitHub 仓库。
- [x] 远端 GitHub Actions CI 已通过（代码提交对应 run `34100336431`）。
- [ ] 完成体验版及正式发布平台材料。

## 最终完成定义

## 2026-09-08 收尾实施阶段（代码与模拟器已完成）

1. 修复按文本顺序解释过敏原声明、旧快照的自由文本调味提示，以及单次候选份量超限导致换菜卡住的问题。
2. 把跨页面的本地读改写放入统一事务队列；验证同时保存、失败恢复和清除数据的顺序。
3. 改善档案保存入口和小屏排版，并使用微信开发者工具自动化逐页验证真实 WXML 交互、持久化、重新进入和错误恢复（已完成）。
4. 保存可复现的模拟器脚本和证据，更新 README/检查清单，完成全量测试、代码审查、GitHub 提交和新版体验包（代码、文档、测试和模拟器证据已完成，并纳入本轮提交）。
5. 真机扫码及手机断网、平台审核/发布由实际设备和平台状态验证；不得以模拟器或本地测试代替。

## 最终完成定义（继续沿用）

- 核心功能实现且满足全部产品不变量。
- 自动化测试、类型检查和静态项目检查全部通过。
- 微信开发者工具可正常启动，并在真实 AppID 下走通核心离线流程。
- README、架构、隐私和发布文档与实际一致。
- 源码安全推送到目标仓库，远端 CI 通过。

当前代码、真实 AppID 配置、开发者工具启动、模拟器核心流程、体验版上传和仓库交付材料已经完成；尚不能宣称已完成手机真机验证、微信审核或正式发布。具体下一步见 `TODO.md` 和 `RELEASE_CHECKLIST.md`。
