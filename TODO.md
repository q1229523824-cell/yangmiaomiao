# 待办

核心代码和本地自动化验证已经完成。后续按以下顺序推进，不要把尚未执行的模拟器或真机步骤标记为完成。

## 0.2.0：外卖怎么点（2026-09-12）

- [x] 实现9种本地点单组合、两位成员午/晚餐筛选、营养区间与未达标差异。
- [x] 主食材/潜在调料禁忌过滤、过敏暂停推荐、无匹配不放宽约束。
- [x] 原生页面、首页入口、收藏、搜索/点单建议复制、按餐保存参考。
- [x] schema 3 及旧存储/备份迁移，数据上限、共享事务和保存失败恢复。
- [x] 全量333项测试、类型检查、5 WXML/6 WXSS 编译及终端估算预览通过。
- [x] 新版本完整模拟器19/19回归及截图检查通过，原数据恢复，无应用异常。剪贴板采用隔离模拟，网络采用API故障注入。
- [ ] 在 iPhone/Android 验证外卖页面、真实剪贴板和首次加载后的飞行模式操作。
- [x] 上传0.2.0开发版本（新AppID，204,043字节）。
- [ ] 在公众平台确认0.2.0体验版选择及手机扫码结果；正式审核/发布另行记录。

## 已完成的本地预验收

- [x] 页面控制器冷启动：首次生成后模块重载、再次 `onShow` 不换菜单也不重复写入。
- [x] 页面持久化：番茄硬排除、档案旧快照、购物勾选、过敏历史拒绝和损坏缓存恢复入口。
- [x] 解析边界：不过敏局部解除、共享过敏后缀、减量语义、故障描述和复合词不误识别。
- [x] 六种已建模过敏原的 64 种组合跨 32 个固定种子；无蛋早餐和麸质保守过滤已加入。
- [x] 目标/份量能力门禁和 60 份历史存储预算回归已加入。

## P0：微信开发者工具验收

- [x] 微信开发者工具 `2.02.2608060` 的官方 `wcc/wcsc` 检查通过：4 个 WXML、5 个 WXSS。
- [x] 配置新小程序 AppID `wxc4ab98745c2f2533`，并由新账号管理员登录微信开发者工具；CLI `islogin` 返回 `login: true`。
- [x] 用新 AppID 导入仓库根目录并执行 `cli open --project`；上传输出确认使用新账号。
- [x] 在开发者工具自动化模式中完成项目启动/编译并记录结果（16 项交互检查全部通过，含本地备份恢复）。
- [ ] 使用新 AppID 生成手机预览二维码。
- [x] 在模拟器依次验证：首次进入、再次进入不换菜、主动换菜、跨日期旧菜单提示。
- [x] 验证偏好：不要番茄、不要鱼但想吃猪里脊、重复偏好、移除单项和重置普通偏好。
- [x] 验证过敏原：牛奶/海鲜过敏、明确取消、普通重置保留，以及不安全历史菜单不可被误用。
- [x] 验证档案：空值、越界值、小数、活动量、目标强度、保存和主动重新计算。
- [x] 验证购物勾选、历史展示、确认恢复、恢复偏好和清除全部本地数据。
- [x] 在模拟器内注入所有网络 API 失败后重复档案、排餐、购物和历史核心流程；真机飞行模式仍待验收。

## P0：真机与体验版

- [x] 2026-09-08 使用新 AppID 上传开发版本 `0.1.2`（152,925 字节），在新公众平台设置为体验版并生成二维码；备注为“离线双人健身饮食体验版（本地备份恢复）”，入口 `pages/index/index`，页面提示有效至 2026-09-15 前。
- [x] 在新账号公众平台确认 `0.1.2` 为体验版并生成二维码。
- [ ] 在新账号添加体验成员 `yxr_6211`，再进行手机扫码、离线和触控回归。
- [ ] iPhone 完成首次加载后断网复测，并记录机型、微信版本和结果。
- [ ] Android 完成首次加载后断网复测，并记录机型、微信版本和结果。
- [ ] 检查小屏、长称呼、大字体、深色背景和触控区域。
- [ ] 确认清理微信存储后的数据丢失提示符合实际。

## P0：平台资料

- [x] 已在新账号后台提交与饮食计划、身高体重记录和购物清单匹配的服务类目。
- [x] 核对类目页面最终状态；用户确认服务类目已审核通过。
- [ ] 完成隐私指引、备案提示及其他平台必填材料。

## P0：GitHub 交付

- [x] 逐项检查 `git status` 和暂存 diff，确认没有凭据、个人临时路径或本地私有配置。
- [x] 将源码默认身高/体重替换为明确虚构、可编辑的演示值，避免公开原型中的真人数据。
- [x] 将本地分支对齐为 `main`，绑定目标空仓库并使用普通 push 完成首次推送；未使用 force push。
- [x] GitHub Actions 的 `npm ci` 和 `npm run verify` 已通过（提交 `fb12d39`，run `34185028324`）。
- [x] 提交 `24a5cae` 与文档提交 `5976a6f` 对应的 GitHub Actions 已通过（run `34204480796`、`34204535800`）。
- [ ] 由项目所有者决定是否添加开源许可证。

## 暂不属于离线第一版

- 登录、支付、订阅消息。
- 云数据库、自动云备份和两台手机自动同步（当前仅支持用户主动复制的本地剪贴板备份）。
- 真实大模型调用。
- 医疗诊断或疾病饮食建议。
