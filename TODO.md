# 待办

核心代码和本地自动化验证已经完成。后续按以下顺序推进，不要把尚未执行的模拟器或真机步骤标记为完成。

## P0：微信开发者工具验收

- [x] 微信开发者工具 `2.02.2608060` 的官方 `wcc/wcsc` 检查通过：4 个 WXML、5 个 WXSS。
- [ ] 准备真实小程序 AppID，并由有权限的成员登录微信开发者工具。
- [ ] 确认 CLI `islogin` 返回已登录；当前结果为未登录。
- [ ] 导入仓库根目录，确认 `miniprogramRoot` 和 TypeScript 编译设置生效。
- [ ] 用真实 AppID 再执行完整 `cli open`；当前占位 AppID 明确返回 code 10：“不存在此 AppID”。
- [ ] 在模拟器依次验证：首次进入、再次进入不换菜、主动换菜、跨日期旧菜单提示。
- [ ] 验证偏好：不要番茄、不要鱼但想吃猪里脊、重复偏好、移除单项和重置普通偏好。
- [ ] 验证过敏原：牛奶/海鲜过敏、明确取消、普通重置保留，以及不安全历史菜单不可被误用。
- [ ] 验证档案：空值、越界值、小数、活动量、目标强度、保存和主动重新计算。
- [ ] 验证购物勾选、历史展示、确认恢复、恢复偏好和清除全部本地数据。
- [ ] 在模拟器断网后重复档案、排餐、购物和历史核心流程。

## P0：真机与体验版

- [ ] 使用真实 AppID 上传体验版。
- [ ] iPhone 完成首次加载后断网复测，并记录机型、微信版本和结果。
- [ ] Android 完成首次加载后断网复测，并记录机型、微信版本和结果。
- [ ] 检查小屏、长称呼、大字体、深色背景和触控区域。
- [ ] 确认清理微信存储后的数据丢失提示符合实际。

## P0：GitHub 交付

- [ ] 逐项检查 `git status` 和暂存 diff，确认没有凭据、个人临时路径或本地私有配置。
- [x] 将源码默认身高/体重替换为明确虚构、可编辑的演示值，避免公开原型中的真人数据。
- [ ] 将本地分支对齐为 `main`，绑定目标空仓库后使用普通 push；不要 force push。
- [ ] 确认 GitHub Actions 的 `npm ci` 和 `npm run verify` 通过。
- [ ] 由项目所有者决定是否添加开源许可证。

## 暂不属于离线第一版

- 登录、支付、订阅消息。
- 云数据库、备份和两台手机自动同步。
- 真实大模型调用。
- 医疗诊断或疾病饮食建议。
