# 隐私说明（离线第一版）

更新时间：2026-09-07

## 本地处理的数据

为了生成双人菜单，小程序会在当前设备保存：

- 两位成员填写的称呼、身高、体重、年龄、活动量和目标强度；
- 饮食偏好、明确排除和过敏原设置；
- 当前菜单及最多 60 份历史菜单快照；
- 每份当前菜单对应的购物清单勾选；
- 本地缓存版本和保存时间。

这些信息用于本机营养估算、排餐、展示、历史恢复和购物清单，不用于医疗诊断。

## 不会执行的行为

当前版本：

- 不创建用户账号；
- 不把上述数据上传到项目自建服务器；
- 不调用真实大模型或第三方分析接口；
- 不包含广告、遥测或用户行为统计 SDK；
- 不读取通讯录、位置、照片、麦克风或相机；
- 不实现夫妻两台设备之间的自动同步。

源码的静态测试会扫描小程序 TypeScript，避免引入 `wx.request`、上传、下载或 WebSocket 调用。但微信本身首次分发和更新小程序代码包仍属于平台网络行为，不是本应用的离线业务处理。

## 保存期限与删除

数据保留在微信为该小程序分配的本地存储中，直到发生以下任一情况：

- 用户在“档案”页确认“清除这台设备上的全部数据”；
- 用户通过微信系统清理小程序数据；
- 卸载、系统回收或其他平台行为移除本地存储。

应用内清除操作只删除 `fitness-couple:` 命名空间下由本项目创建的数据。当前没有云端副本可以恢复，清除后通常不可找回。

如果本地缓存内容损坏、迁移失败或来自更新版本，应用不会用默认档案静默覆盖它；会停止读取并提示恢复入口。只有用户在“档案”页明确确认后，才会清除这份本地数据并重新开始。

## 设备与分享风险

- 同一设备上能够打开该小程序的人可能看到本地档案和历史菜单。
- 两台设备的数据彼此独立，不应假设一台设备的修改会同步到另一台。
- 向他人发送调试包、截图或导出的本地数据前，应自行检查其中是否含身体数据。

## 健康信息边界

热量和三大营养素基于常见食材数值与公式估算，会受到品牌、烹饪方式、可食部和记录误差影响。小程序不提供诊断、治疗或个体化临床营养建议；有疾病、妊娠、进食障碍、严重过敏或特殊营养需求时，应咨询医生或注册营养师。

过敏原规则只检查目录中已结构化标注的主要食材。调味料配方、不同品牌标签、生产线和厨房交叉污染不在当前模型内，用户仍需自行核对包装标签与烹饪环境。

## 开发和发布安全

- 仓库中的 `project.config.json` 使用项目所有者提供的 AppID 作为公开项目标识；AppID 不是账号密钥。
- AppSecret、访问令牌、私钥及真实测试数据不得写入小程序前端或提交到仓库。
- 源码只包含明确虚构、可编辑的默认档案；用户实际填写的身体数据仅进入当前设备本地存储，不提交到仓库。
- `project.private.config.json`、环境文件和常见密钥文件已列入 `.gitignore`。
- 如果未来加入登录、云同步、分析、模型或第三方服务，必须在上线前更新本说明，披露接收方、目的、保存期限、权限和删除方式，并完成微信平台要求的用户同意流程。
